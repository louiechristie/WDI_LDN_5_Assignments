<!DOCTYPE html>
<html>
<head>
  <title>disastronomia</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <link href='http://fonts.googleapis.com/css?family=Metrophobic' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Roboto:300' rel='stylesheet' type='text/css'>
  <%= csrf_meta_tags %>
</head>
<body>
  <header>
    <div class="bar">
      The definitive cookbook for serving disaster, apocalyptic ruin, and pandemonium in epic proportions.
    </div>
      <div class="logo">
        <h1>disastro</h1>
        <h1>
          <a href="/">
            <div class="box"></div>
          </a>
        nomia</h1>
      </div>
  </header>
  <ul>
    <% if current_user %>
      <li>Welcome, <%= current_user.email %>! | 
        <%= link_to "Logout", session_path(0), method: :delete %></li>
    <% else %>
      <li><%= link_to "Login", new_session_path, class: active_menu_item(new_session_path) %> | 
        <%= link_to "Join", new_user_path, class: active_menu_item(new_user_path) %></li>
    <% end %>
    <li><%= link_to "Categories#index", categories_path, class: active_menu_item(categories_path) %></li>
    <li><%= link_to "Recipes#index", recipes_path, class: active_menu_item(recipes_path) %></li>
    <li><%= link_to "Ingredients#index", ingredients_path, class: active_menu_item(ingredients_path) %></li>
    <li><%= link_to "Users#index", users_path, class: active_menu_item(users_path) %></li>
  </ul>
  <br />
  <br />
  <div class="flash">
    <h2>
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
    </h2>
  </div>
  <div class="yield">
    <%= yield %>
  </div>
</body>
<footer>
</footer>
</html>
