<h1>Categories#index</h1>
<%= link_to "New Category", new_category_path %>
<% if @categories %>
  <ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.name, category_path(category) %> <%= image_tag("#{category.image}") if category.image > "" %></li>
  <% end %>
  </ul>
<% end %>
