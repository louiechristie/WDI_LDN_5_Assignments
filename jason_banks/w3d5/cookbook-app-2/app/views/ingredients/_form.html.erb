<%= form_for @ingredient do |f| %>
<% if @ingredient.errors.any? %>
    <div id="error_explanation">
      <h3><%= pluralize(@ingredient.errors.count, "error") %> prohibited this ingredient from being saved: <%= @ingredient.errors.full_messages.join(", ").downcase %>.</h3>
    </div>
  <% end %>

<%= f.label :name %>
<div class="black"><%= f.text_field :name %></div><br /><br />
<div class="black"><%= submit_tag("Submit") %></div>
<% end %>
<%= link_to "Cancel", ingredients_path %>
