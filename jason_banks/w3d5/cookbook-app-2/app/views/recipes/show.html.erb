<h2><%= @recipe.title %></h2>
<%= image_tag("#{@recipe.image}") %><br />
<%= button_to "Edit #{@recipe.title}", edit_recipe_path(@recipe), method: "get" %>
<%= button_to "Delete #{@recipe.title}", delete_recipe_path(@recipe), method: "post", confirm: "Delete #{@recipe.title}?" %>
