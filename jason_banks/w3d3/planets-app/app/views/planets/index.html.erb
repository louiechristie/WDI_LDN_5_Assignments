<% @planets.each do |planet| %>
  <p><%= link_to planet.name, planet_path(planet) %> has <%= pluralize(planet.moons, 'moon') %>.<%= button_to 'Delete Planet', planet_path(planet), method: 'delete', confirm: "Are you sure you want to delete #{planet.name}?" %></p>
<% end %>

