<h2>Cookbook Recipes</h2>

<h3><a class='new' href="/recipes/new">New Recipe</a></h3>

<table>
		<tr>
			<th>Name</th>
		</tr>
		<% @recipes.each do |recipe| %>
		<tr>
			<td><%= recipe.name %></td>
      <td><%= recipe.size %></td>
      <td><%= recipe.category.name %></td>
			<td><%= link_to 'Show', recipe_path(recipe) %></td><td><%= link_to'Edit', edit_recipe_path(recipe) %></td><td><%= link_to 'Delete', recipe, method: :delete, data: { confirm: 'Are you sure?' } %></td></tr>
		<% end %>
</table>


