<h1><%= @category.name %></h1>


<% if can? :delete, category %><%= button_to 'Delete Category', category_path(@category), method: 'delete', confirm: 'Are you sure?' %><% end %>
<% if can? :update, category %><%= button_to 'Edit Category', edit_category_path(@category), method: 'get' %><% end %>