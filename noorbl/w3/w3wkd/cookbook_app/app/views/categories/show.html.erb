<h1><%= @category.name %></h1>


<%= button_to 'Delete Category', category_path(@category), method: 'delete', confirm: 'Are you sure?' %>
<%= button_to 'Edit Category', edit_category_path(@category), method: 'get' %>