<h2>Categories</h2>

<ul>
<% @categories.each do |category| %>

    <li><%= link_to(category.name, category_path(category.id)) %>
      <%= link_to('Delete', category, :method => :delete) %> </li>



<% end %>

</ul>
