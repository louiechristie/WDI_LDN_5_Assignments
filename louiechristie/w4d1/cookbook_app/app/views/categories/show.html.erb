<p id="notice"><%= notice %></p>
<h2>Category</h2>
<p>Name: <%= @category.name %></p>

<p><%= link_to('Edit', edit_category_path(@category.id)) %> <%= link_to('Delete', category_path(@category.id), :method => :delete) %></p>