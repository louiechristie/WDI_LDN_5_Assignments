<%= form_for(@ingridient) do |i| %>
  <% if @ingridient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ingridient.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @ingridient.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= i.label :name %><br />
    <%= i.text_field :name %>
  </div>
  <div class="actions">
    <%= i.submit %>
  </div>
<% end %>