<%= form_for(@car) do |c| %>
  <% if @car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@car.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @car.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= c.label :name %><br />
    <%= c.text_field :name %>
  </div>
  <div class="field">
    <%= c.label :image %><br />
    <%= c.text_field :image %>
  </div>
  <div class="field">
    <%= c.label :color %><br />
    <%= c.text_field :color %>
  </div>
  <div class="field">
    <%= c.label :description %><br />
    <%= c.text_area :description %>
  </div>
  <div class="field">
    <%= c.label :year %><br />
    <%= c.select :year, options_for_select(2014.downto(1900)) %>
  </div>
  <div class="field">
    <%= c.label :engine_id %><br />
    <%= c.select :engine_id, options_from_collection_for_select(Engine.all, :id, :name) %>
  </div>
  <div class="actions">
    <%= c.submit %>
  </div>
<% end %>
