<h2>index.html.erb</h2>

 <h2>

</h2>


<%= @categories.each do |category| %>

  <%= category.name %>

  <%= link_to "edit", edit_category_path(category)%>


<p> <%= link_to 'Delete', category_path(category), method: 'delete', confirm: 'Are you sure?' %>  </p>

  <%= link_to "New category", new_category_path(category)%>

  <% end %>