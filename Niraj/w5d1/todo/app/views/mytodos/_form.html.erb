<%= form_for(@mytodo) do |f| %>
  <% if @mytodo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mytodo.errors.count, "error") %> prohibited this mytodo from being saved:</h2>

      <ul>
      <% @mytodo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
