
<p><%= "#{@ingredient.name}"</p>   

flsaj

<% if can? :update, @ingredient %>
  <%= button_to 'Delete', recipe_path(@ingredient), :method => :delete %>
  <%= button_to 'Edit', edit_recipe_path(@ingredient), :method => :get %>
<% end %>